import { BrandLogo } from '../components/BrandLogo';

export function DashboardPage() {
  return (
    <main className="dashboard-placeholder">
      <section>
        <BrandLogo className="dashboard-logo" />
        <p>Dashboard page in progress</p>
      </section>
    </main>
  );
}
